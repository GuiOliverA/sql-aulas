--Relatório de Altura: Quem é a pessoa mais alta (MAX) e a mais baixa (MIN) da sua tabela?

SELECT MAX(altura), MIN(altura) from dados;

--Selecionar o maior (registro da coluna altura), o menor (registro da coluna altura) da tabela dados;